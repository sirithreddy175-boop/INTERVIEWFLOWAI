// Shared between the engine (server) and the UI (client).
/** Hard ceiling on substantive questions in one interview. */
export const MAX_QUESTIONS = 8;
/** Minimum evidence before the interview may finish. */
export const MIN_QUESTIONS = 6;
/** Used by the UI progress indicator. */
export const TOTAL_QUESTIONS = MAX_QUESTIONS;
